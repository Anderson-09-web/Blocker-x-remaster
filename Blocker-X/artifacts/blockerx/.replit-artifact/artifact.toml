kind = "web"
previewPath = "/"
title = "Blocker X"
version = "1.0.0"
id = "artifacts/blockerx"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/blockerx run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/blockerx", "run", "build" ]
publicDir = "artifacts/blockerx/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
